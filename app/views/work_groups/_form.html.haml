= form_for @work_group do |f|
  -if @work_group.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@work_group.errors.count, "error")} prohibited this work_group from being saved:"
      %ul
        - @work_group.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .field
    = f.label :description
    = f.text_field :description
  .field
    = f.label :initiative
    = f.text_field :initiative
  .actions
    = f.submit 'Save'

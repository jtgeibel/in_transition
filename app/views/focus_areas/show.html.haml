%h1= @focus_area.name
.box
  %h2 Work Groups
  - @focus_area.work_groups.each do |group|
    = link_to "#{group.initiative.name}: #{group.name}", group
.box
  %h2 Resources
  -@focus_area.resources.each do |resource|
    = link_to resource.title, resource
.box
  %h2 People
  (coming soon)
- content_for :sidebar do
  .box
    %h1 Featured
    (none here yet)
